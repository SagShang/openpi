# RobotWin Examples

This directory groups the RobotWin-specific OpenPI example scripts:

- `convert_robotwin_to_lerobot.py`
- `export_lerobot_episode_video.py`
- `serve_robotwin_policy.py`

The legacy entrypoints under `scripts/` are kept as thin compatibility wrappers.
